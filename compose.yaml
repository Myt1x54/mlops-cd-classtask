services:
  mlops-api:
    build:
      context: .
      args:
        APP_VERSION: "1.0.0"
        GIT_COMMIT: "local-compose"
    image: mlops-cd-demo:local
    container_name: mlops-api
    restart: unless-stopped
    ports:
      - "5000:5000"
